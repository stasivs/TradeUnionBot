version: '3.8'
services:
  bot:
    build: .
    restart: always
    depends_on:
      - redis
    networks:
      - profkom_bot_net

  redis:
    image: redis
    restart: always
    networks:
      - profkom_bot_net

networks:
  profkom_bot_net:
    external:
      name: profkom_server_net
