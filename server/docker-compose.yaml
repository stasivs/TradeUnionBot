version: '3'
services:
  nginx:
    image: nginx:alpine
    restart: always
    ports:
      - '80:80'
    volumes:
      - ./nginx_config:/etc/nginx/conf.d
    depends_on:
      - app

  app:
    build: ./app
    restart: always
    depends_on:
      - mongodb

  mongodb:
    image: mongo
    restart: always
    volumes:
      - data:/data/db

volumes:
  data: